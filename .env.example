# dddd-next secrets template — copy to .env and fill in real values.
# .env is gitignored; this .env.example is committed as documentation.
#
# 所有配置均可选；测绘密钥仅在对应搜索查询中使用。
# GITHUB_TOKEN 仅在显式检查或更新本体时使用。

# GitHub — 可选，提高 Release API 的请求限额。
# 仅发送到本体更新的 API 请求，不发送到下载地址或重定向目标。
# 访问本公开仓库无需授予仓库写权限。
GITHUB_TOKEN=

# FOFA — 官方与兼容 FOFA 协议的自建服务，均需配置邮箱和 Key。
# 留空默认 https://fofa.info；填写服务器基础地址，可带路径前缀。
# 例如 https://fofa-gateway.example.com/fofa（不要填写完整搜索接口、查询参数或凭证）。
# 程序自动追加 /api/v1/search/all。自建地址使用该服务对应的邮箱和 Key。
FOFA_SERVER=
FOFA_EMAIL=
FOFA_KEY=

# Hunter — hunter.qianxin.com
HUNTER_API_KEY=

# Quake — quake.360.net
QUAKE_TOKEN=
