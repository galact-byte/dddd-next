## ✨ 新增功能
- `dddd update -nt <目录>` 可指定并记住 nuclei 模板根目录；首次成功更新后，后续无参数 `dddd update` 会自动更新同一目录。
- 扫描会自动使用已记住的模板目录；`-nt <目录>` 可仅对当前扫描临时覆盖。

## 🛠️ 修复与优化
- 仅在模板成功 clone 或 pull 后保存新目录，更新失败不会覆盖原有设置。
- 精准 POC 模式继续保留内置 legacy POC 回退，避免外部模板目录导致项目自带专有 POC 缺失。
- Windows 下内置配置包校验统一按仓库 LF 规范比较，避免 CRLF 工作树产生误报。

## 🔄 兼容性说明
- 未配置自定义目录时，仍使用原有默认目录逻辑。
- 用户设置保存在 Windows 的 `%AppData%\dddd-next\templates.json`（其他平台位于系统用户配置目录）。
