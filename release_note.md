## 🛠️ 修复与优化

- `update` 首次下载和后续更新模板时，实时显示 Git 的分阶段百分比进度；传输阶段可查看下载量和速度，避免长时间无输出而误以为下载卡住。
- 下载失败时即时显示 Git 错误，最终结果仍保留失败原因。

## 🔄 兼容性说明

- 原有 `update -nt <目录>` 用法和模板目录记忆功能保持不变，无需增加参数。
- 连接服务器、尚未开始传输时，需等待 Git 提供下载进度。
